import { Test } from '@nestjs/testing';
import { AppModule } from 'src/app.module';

describe('App e2e', () => {

  // beforeAll(async () => {

  //   //first check app.module 
  //   const moduleRef =
  //     await Test.createTestingModule({
  //       imports: [AppModule],
  //     }).compile();
  // });

  it.todo('should pass')
});